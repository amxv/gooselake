{
  "schema_revision": "gooseweb-network-allowlist/v2",
  "exact_http": [
    {
      "method": "GET",
      "path": "/",
      "status": 200
    },
    {
      "method": "POST",
      "path": "/api/dev-ticket",
      "status": 200
    }
  ],
  "exact_websocket_events": [
    {
      "event": "open",
      "code": 0
    }
  ],
  "policy": "Captures are compared as exact method/path/status and event/code multisets with query strings and secrets excluded at capture. Unexpected HTTP, protocol, network, or WebSocket activity fails. Unavailable WebSocket capture requires an explicit blocked baseline mapping and prohibits inference."
}
