# Example optional environment overrides for gg-goosetower.
# Copy to: ~/.config/gg-goosetower/goosetower.env
#
# Keep runtime bearer tokens and ticket signing keys in files referenced by
# goosetower.toml. Do not expose the Gooselake runtime token to Gooseweb.

RUST_LOG=info,goosetower=debug
